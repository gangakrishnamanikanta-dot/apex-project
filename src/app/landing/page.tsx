'use client';

import LandingPage from '../page';

/**
 * MEERASH Public Website: Landing Page Route (`/landing`)
 * Re-exports the primary LandingPage component for explicit route matching.
 */
export default function LandingRoute() {
  return <LandingPage />;
}
